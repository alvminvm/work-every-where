" Press `f` to activate AceJump
"nmap f :action AceAction<CR>
" Press `F` to activate Target Mode
"nmap F :action AceTargetAction<CR>
" Press `g` to activate Line Mode
"nmap g :action AceLineAction<CR>

nmap f :action KJumpAction<CR>

" Enter -> o
nmap <CR> o
unmap <S-CR>

" stirng manipulation
vmap _ <action>(StringManipulation.ToSnakeCase)
vmap - <action>(StringManipulation.ToKebabCase)

" 使用 idea 的 join 功能。例如自动移除大括号 
set ideajoin